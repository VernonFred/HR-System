# ============================================
# QZ·TalentLens 环境变量配置模板
# 使用说明：复制此文件为 .env，并根据实际情况修改配置项
# ============================================

# ---------- 数据库配置 ----------
# 方案A: SQLite（默认，适合小规模部署，每天面试 < 10人）
DATABASE_URL=sqlite:///./hr.db

# 方案B: PostgreSQL（企业级部署，支持高并发和多服务器）
# DATABASE_URL=postgresql://用户名:密码@主机:5432/数据库名
# 示例: DATABASE_URL=postgresql://talentlens_user:YourStrongPassword123!@db.company.com:5432/talentlens_prod

# 是否输出 SQL 日志（调试用，生产环境建议设为 false）
SQL_ECHO=false

# ---------- AI 服务配置 ----------
# 统一使用 DeepSeek 单模型，不再配置 ModelScope / 硅基流动备用模型
AI_API_KEY=your_deepseek_api_key_here
AI_API_BASE=https://api.deepseek.com
AI_MODEL=deepseek-v4-pro
AI_STREAM=true
AI_TIMEOUT=120

# ---------- 密码安全配置 ----------
# 密码哈希盐值（生产环境必须修改！一旦设置不可更改，否则所有密码失效）
# 生成方法: openssl rand -hex 16
AUTH_SALT=change-this-to-a-random-string

# ---------- JWT 认证配置 ----------
# JWT 密钥（生产环境必须修改为强随机字符串！）
# 生成方法: openssl rand -hex 32
JWT_SECRET_KEY=your-super-secret-jwt-key-please-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=10080

# Refresh Token 过期时间（分钟）
REFRESH_TOKEN_EXPIRATION_MINUTES=20160

# ---------- 应用配置 ----------
# 应用名称
APP_NAME=QZ·TalentLens

# 日志级别（DEBUG, INFO, WARNING, ERROR, CRITICAL）
LOG_LEVEL=INFO

# CORS 允许的来源（多个用逗号分隔，生产环境限制为实际域名）
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,https://your-domain.com

# ---------- 文件上传配置 ----------
# 最大上传文件大小（MB）
MAX_UPLOAD_SIZE_MB=10

# 上传文件存储路径（相对于 backend 目录）
UPLOAD_DIR=./uploads

# ---------- 管理员账号配置（首次启动自动创建）----------
# 管理员用户名（生产环境建议修改）
ADMIN_USERNAME=admin

# 管理员密码（生产环境必须修改！）
ADMIN_PASSWORD=admin123

# ============================================
# 安全提醒：
# 1. 不要将此文件命名为 .env 后提交到 Git
# 2. 生产环境必须修改所有密码和密钥
# 3. 定期更换 JWT 密钥和管理员密码
# 4. 限制 CORS_ORIGINS 为实际使用的域名
# ============================================

# ============================================
# 配置步骤：
# 1. 复制此文件：cp .env.example .env
# 2. 编辑 .env 文件：nano .env
# 3. 根据实际情况修改配置项
# 4. 启动服务：uvicorn app.main:app --reload
# ============================================
