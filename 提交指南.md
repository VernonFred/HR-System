# Git 提交指南

## 📋 提交前准备

### 1. 确认要提交的内容

**核心代码（必须提交）**：
- ✅ `backend/app/` - 后端核心代码
- ✅ `backend/alembic/` - 数据库迁移
- ✅ `backend/pyproject.toml` - Python依赖
- ✅ `backend/poetry.lock` - 依赖锁定
- ✅ `backend/.env.example` - 环境变量示例
- ✅ `backend/alembic.ini` - Alembic配置
- ✅ `frontend/` - 前端代码
- ✅ `docs/` - 项目文档
- ✅ `docker-compose.yml` - Docker配置
- ✅ `start_for_demo.sh` - 演示启动脚本
- ✅ `stop_demo.sh` - 演示停止脚本
- ✅ `演示启动指南.md` - 使用说明

**不提交（已在.gitignore）**：
- ❌ `backend/hr.db` - 数据库文件
- ❌ `backend/.env` - 环境变量（含敏感信息）
- ❌ `backend/uploads/` - 上传文件
- ❌ `backend/test_*.py` - 测试脚本
- ❌ `backend/create_*.py` - 数据生成脚本
- ❌ `.vscode/` - IDE配置
- ❌ `.DS_Store` - Mac系统文件
- ❌ `node_modules/` - 前端依赖（通过package.json安装）

---

## 🚀 提交步骤

### 方式一：手动提交（推荐）

```bash
# 1. 进入项目目录
cd /Users/Python项目/HR人事

# 2. 查看当前状态
git status

# 3. 删除旧的已删除文件记录
git add -u

# 4. 添加新文件
git add backend/app/
git add backend/alembic/
git add backend/pyproject.toml
git add backend/poetry.lock
git add backend/.env.example
git add backend/alembic.ini
git add backend/.flake8
git add backend/pre_deploy_check.sh
git add frontend/
git add docs/
git add docker-compose.yml
git add start_for_demo.sh
git add stop_demo.sh
git add 演示启动指南.md
git add .gitignore

# 5. 查看将要提交的内容
git status

# 6. 提交（含中文提交信息）
git commit -m "feat: HR人事系统核心功能完成

主要更新:
- 完成后端API系统(FastAPI + SQLModel)
- 完成前端界面(Vue3 + Element Plus)
- 实现候选人画像功能
- 实现AI分析集成
- 添加测评分发链接功能
- 完成数据库设计和迁移
- 添加文档和部署脚本

技术栈:
- 后端: Python 3.9+, FastAPI, SQLModel, SQLAlchemy
- 前端: Vue 3, TypeScript, Vite, Element Plus
- 数据库: SQLite (可迁移到MySQL)
- AI: Claude API集成"

# 7. 推送到GitHub
git push origin master
```

### 方式二：使用脚本提交

创建快捷脚本 `git-commit.sh`:

```bash
#!/bin/bash

echo "🔍 检查Git状态..."
git status

echo ""
echo "📝 删除旧文件记录..."
git add -u

echo ""
echo "➕ 添加新文件..."
git add backend/app/ backend/alembic/ backend/pyproject.toml backend/poetry.lock backend/.env.example backend/alembic.ini backend/.flake8 backend/pre_deploy_check.sh
git add frontend/
git add docs/
git add docker-compose.yml start_for_demo.sh stop_demo.sh 演示启动指南.md .gitignore

echo ""
echo "📊 将要提交的文件:"
git status

echo ""
read -p "确认提交? (y/n): " confirm
if [ "$confirm" = "y" ]; then
    echo ""
    echo "💾 提交中..."
    git commit -m "feat: HR人事系统核心功能完成

主要更新:
- 完成后端API系统(FastAPI + SQLModel)
- 完成前端界面(Vue3 + Element Plus)
- 实现候选人画像功能
- 实现AI分析集成
- 添加测评分发链接功能
- 完成数据库设计和迁移
- 添加文档和部署脚本

技术栈:
- 后端: Python 3.9+, FastAPI, SQLModel, SQLAlchemy
- 前端: Vue 3, TypeScript, Vite, Element Plus
- 数据库: SQLite (可迁移到MySQL)
- AI: Claude API集成"
    
    echo ""
    echo "🚀 推送到GitHub..."
    git push origin master
    
    echo ""
    echo "✅ 提交完成!"
else
    echo "❌ 取消提交"
fi
```

使用方法:
```bash
chmod +x git-commit.sh
./git-commit.sh
```

---

## ⚠️ 注意事项

### 1. 敏感信息保护

确保以下文件**不要提交**:
- `backend/.env` - 包含API密钥
- `backend/hr.db` - 数据库文件
- `backend/uploads/*` - 用户上传文件

### 2. 首次推送到新仓库

如果是首次推送到GitHub:

```bash
# 1. 在GitHub创建新仓库 (不要初始化README)

# 2. 添加远程仓库
git remote add origin https://github.com/你的用户名/HR人事系统.git

# 3. 推送
git branch -M master
git push -u origin master
```

### 3. 如果已有远程仓库

```bash
# 查看远程仓库
git remote -v

# 如果需要更改远程地址
git remote set-url origin https://github.com/你的用户名/新仓库名.git
```

---

## 🔧 常见问题

### Q1: 推送被拒绝 (rejected)

**原因**: 远程仓库有本地没有的提交

**解决方案**:
```bash
# 先拉取远程更新
git pull origin master --rebase

# 如果有冲突，解决后
git add .
git rebase --continue

# 再推送
git push origin master
```

### Q2: 忘记添加 .gitignore

**解决方案**:
```bash
# 移除已跟踪的敏感文件
git rm --cached backend/.env
git rm --cached backend/hr.db
git rm -r --cached .vscode/

# 提交移除操作
git commit -m "chore: 移除敏感文件"
git push
```

### Q3: 想撤销上次提交

```bash
# 撤销最后一次提交，但保留修改
git reset --soft HEAD~1

# 撤销最后一次提交，丢弃修改（危险！）
git reset --hard HEAD~1
```

---

## 📚 提交信息规范

使用约定式提交（Conventional Commits）:

- `feat:` 新功能
- `fix:` 修复bug
- `docs:` 文档更新
- `style:` 代码格式（不影响功能）
- `refactor:` 重构
- `test:` 测试相关
- `chore:` 构建/工具相关

**示例**:
```bash
git commit -m "feat: 添加候选人批量导入功能"
git commit -m "fix: 修复画像导出时的样式问题"
git commit -m "docs: 更新部署文档"
```

---

## ✅ 提交检查清单

提交前确认:
- [ ] `.gitignore` 已配置
- [ ] 敏感信息已移除（.env, 数据库文件）
- [ ] 代码已测试通过
- [ ] 文档已更新
- [ ] 提交信息清晰
- [ ] 远程仓库地址正确

---

**准备好了？按照上面的步骤开始提交吧！** 🚀

