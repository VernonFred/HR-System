# QZ·TalentLens 运维说明

## 📁 部署包文件说明

```
deploy_complete/
├── backend/
│   ├── app/              # Python 后端代码
│   ├── .env              # ⭐ 环境变量配置（含 API 密钥，必须保密）
│   ├── .env.example      # 环境变量模板
│   ├── hr.db             # ⭐ SQLite 数据库（含现有数据）
│   └── requirements.txt  # Python 依赖
└── frontend/
    └── dist/             # 前端静态文件
```

## ⚠️ 重要文件说明

### 1. `.env` 环境变量文件

**必须拷贝到服务器！**

包含内容：
- AI API 密钥（硅基流动、ModelScope）
- 数据库配置
- JWT 密钥

```bash
# 查看当前配置（不显示密钥值）
cat .env | grep -v "KEY="
```

**安全提示：**
- 此文件包含敏感信息，不要提交到 Git
- 生产环境建议修改 JWT_SECRET_KEY

### 2. `hr.db` 数据库文件

**SQLite 数据库，包含：**
- 用户账户（admin/admin123）
- 专业测评问卷（MBTI、DISC、EPQ）
- 自定义问卷和测评
- 候选人数据
- 岗位画像配置

**迁移选择：**

| 场景 | 是否拷贝 hr.db |
|------|---------------|
| 全新部署，从零开始 | ❌ 不需要，首次启动自动创建 |
| 保留现有数据 | ✅ 需要拷贝 |
| 从开发环境迁移到生产 | ✅ 需要拷贝 |

## 🔧 后续运维

### 数据库备份

```bash
# 备份数据库
cp hr.db hr.db.backup_$(date +%Y%m%d)

# 或使用 sqlite3 导出
sqlite3 hr.db .dump > backup.sql
```

### 更新环境变量

如需更新 API 密钥：

```bash
# 编辑 .env 文件
vim .env

# 重启后端服务使配置生效
pkill -f uvicorn
uvicorn app.main:app --host 0.0.0.0 --port 9000 &
```

### 查看数据库内容

```bash
# 查看所有表
sqlite3 hr.db ".tables"

# 查看用户
sqlite3 hr.db "SELECT id, username, role FROM users;"

# 查看问卷
sqlite3 hr.db "SELECT id, title, type FROM questionnaires;"
```

## 📝 常见问题

### Q: 启动后登录失败 401
检查 `.env` 文件是否存在且配置正确

### Q: AI 分析功能不可用
检查 `.env` 中的 API_KEY 是否有效

### Q: 数据丢失
确保 `hr.db` 文件已正确拷贝到服务器
