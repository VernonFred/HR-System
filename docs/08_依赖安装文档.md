# QZ·TalentLens 依赖安装文档

> **版本**: v2.0  
> **更新日期**: 2025年12月12日  
> **适用环境**: macOS / Linux / Windows

---

## 一、环境要求

### 1.1 系统要求

| 组件 | 最低版本 | 推荐版本 |
|------|----------|----------|
| Python | 3.10+ | 3.11+ |
| Node.js | 16+ | 18+ |
| npm | 8+ | 10+ |
| pip | 21+ | 最新版 |

### 1.2 检查当前环境

```bash
# 检查 Python 版本
python3 --version

# 检查 Node.js 版本
node --version

# 检查 npm 版本
npm --version

# 检查 pip 版本
pip --version
```

---

## 二、后端依赖

### 2.1 依赖清单

| 依赖包 | 版本 | 用途 |
|--------|------|------|
| fastapi | ^0.110.0 | Web 框架 |
| uvicorn | ^0.30.0 | ASGI 服务器 |
| pydantic | ^2.7.0 | 数据验证 |
| sqlmodel | ^0.0.16 | ORM |
| alembic | ^1.13.0 | 数据库迁移 |
| psycopg2-binary | ^2.9.9 | PostgreSQL 驱动 |
| python-dotenv | ^1.0.1 | 环境变量管理 |
| httpx | ^0.27.2 | HTTP 客户端 |
| python-multipart | ^0.0.20 | 文件上传 |
| pdfplumber | ^0.11.8 | PDF 解析 |
| python-docx | ^1.2.0 | Word 文档解析 |
| aiofiles | ^25.1.0 | 异步文件操作 |
| openpyxl | ^3.1.0 | Excel 操作 |

### 2.2 安装方式

#### 方式一：使用 Poetry (推荐)

```bash
# 进入后端目录
cd backend

# 安装 Poetry (如果未安装)
pip install poetry

# 安装依赖
poetry install

# 激活虚拟环境
poetry shell
```

#### 方式二：使用 pip

```bash
# 进入后端目录
cd backend

# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
# macOS/Linux:
source venv/bin/activate
# Windows:
.\venv\Scripts\activate

# 安装依赖
pip install fastapi uvicorn[standard] pydantic sqlmodel alembic
pip install psycopg2-binary python-dotenv httpx python-multipart
pip install pdfplumber python-docx aiofiles openpyxl
```

#### 方式三：使用 requirements.txt

```bash
# 生成 requirements.txt (如果没有)
pip freeze > requirements.txt

# 从 requirements.txt 安装
pip install -r requirements.txt
```

### 2.3 验证安装

```bash
# 验证核心依赖
python -c "import fastapi; print(f'FastAPI: {fastapi.__version__}')"
python -c "import uvicorn; print(f'Uvicorn: {uvicorn.__version__}')"
python -c "import pdfplumber; print('pdfplumber: OK')"
python -c "import docx; print('python-docx: OK')"
```

### 2.4 常见问题

#### pdfplumber 安装失败

```bash
# macOS 需要安装 poppler
brew install poppler

# Ubuntu/Debian
sudo apt-get install poppler-utils

# 然后重新安装
pip install pdfplumber
```

#### psycopg2 安装失败

```bash
# macOS
brew install postgresql

# Ubuntu/Debian
sudo apt-get install libpq-dev python3-dev

# 或使用 binary 版本
pip install psycopg2-binary
```

---

## 三、前端依赖

### 3.1 依赖清单

| 依赖包 | 版本 | 用途 |
|--------|------|------|
| vue | ^3.4.0 | 前端框架 |
| vue-router | ^4.3.0 | 路由管理 |
| pinia | ^2.2.0 | 状态管理 |
| axios | ^1.13.2 | HTTP 客户端 |
| echarts | ^5.4.3 | 图表库 |
| qrcode | ^1.5.4 | 二维码生成 |
| xlsx | ^0.18.5 | Excel 导出 |
| html2canvas | ^1.4.1 | HTML 截图 |
| jspdf | ^3.0.4 | PDF 生成 |
| sortablejs | ^1.15.6 | 拖拽排序 |
| gsap | ^3.13.0 | 动画库 |

### 3.2 开发依赖

| 依赖包 | 版本 | 用途 |
|--------|------|------|
| vite | ^5.0.0 | 构建工具 |
| typescript | ^5.3.0 | TypeScript |
| @vitejs/plugin-vue | ^5.0.0 | Vue 插件 |
| eslint | ^8.57.0 | 代码检查 |
| eslint-plugin-vue | ^9.18.1 | Vue ESLint 插件 |

### 3.3 安装方式

```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install

# 或使用 yarn
yarn install

# 或使用 pnpm
pnpm install
```

### 3.4 验证安装

```bash
# 检查依赖是否完整
npm ls

# 运行开发服务器
npm run dev
```

### 3.5 常见问题

#### node-gyp 编译错误

```bash
# macOS 需要安装 Xcode Command Line Tools
xcode-select --install

# Windows 需要安装 Visual Studio Build Tools
npm install --global windows-build-tools
```

#### 依赖版本冲突

```bash
# 清除缓存重新安装
rm -rf node_modules package-lock.json
npm cache clean --force
npm install
```

---

## 四、完整安装步骤

### 4.1 一键安装脚本 (macOS/Linux)

```bash
#!/bin/bash
# install.sh

echo "=== QZ·TalentLens 依赖安装 ==="

# 检查 Python
if ! command -v python3 &> /dev/null; then
    echo "❌ Python 未安装，请先安装 Python 3.10+"
    exit 1
fi

# 检查 Node.js
if ! command -v node &> /dev/null; then
    echo "❌ Node.js 未安装，请先安装 Node.js 18+"
    exit 1
fi

echo "✅ 环境检查通过"

# 安装后端依赖
echo "📦 安装后端依赖..."
cd backend
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install fastapi uvicorn[standard] pydantic sqlmodel alembic
pip install psycopg2-binary python-dotenv httpx python-multipart
pip install pdfplumber python-docx aiofiles openpyxl
echo "✅ 后端依赖安装完成"

# 安装前端依赖
echo "📦 安装前端依赖..."
cd ../frontend
npm install
echo "✅ 前端依赖安装完成"

echo "=== 安装完成 ==="
```

### 4.2 手动安装步骤

#### 步骤 1：克隆项目

```bash
git clone <repository_url> HR人事
cd HR人事
```

#### 步骤 2：安装后端依赖

```bash
cd backend

# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
source venv/bin/activate  # macOS/Linux
# 或
.\venv\Scripts\activate  # Windows

# 升级 pip
pip install --upgrade pip

# 安装依赖
pip install fastapi uvicorn[standard] pydantic sqlmodel alembic
pip install psycopg2-binary python-dotenv httpx python-multipart
pip install pdfplumber python-docx aiofiles openpyxl

# 验证
python -c "import fastapi; print('FastAPI OK')"
```

#### 步骤 3：配置环境变量

```bash
# 复制示例配置
cp .env.example .env

# 编辑配置
nano .env
```

#### 步骤 4：安装前端依赖

```bash
cd ../frontend

# 安装依赖
npm install

# 验证
npm run dev
```

---

## 五、依赖更新

### 5.1 后端依赖更新

```bash
cd backend
source venv/bin/activate

# 更新所有依赖
pip install --upgrade -r requirements.txt

# 或使用 Poetry
poetry update
```

### 5.2 前端依赖更新

```bash
cd frontend

# 检查过时依赖
npm outdated

# 更新依赖
npm update

# 更新到最新版本 (谨慎)
npm install <package>@latest
```

### 5.3 安全更新

```bash
# 后端安全检查
pip install safety
safety check

# 前端安全检查
npm audit
npm audit fix
```

---

## 六、开发环境启动

### 6.1 启动后端

```bash
cd backend
source venv/bin/activate

# 开发模式
uvicorn app.main:app --reload --host 0.0.0.0 --port 9000
```

### 6.2 启动前端

```bash
cd frontend

# 开发模式
npm run dev
```

### 6.3 访问系统

- 前端: http://localhost:5173
- 后端 API: http://localhost:9000
- API 文档: http://localhost:9000/docs

---

## 七、生产环境依赖

### 7.1 额外生产依赖

```bash
# 后端
pip install gunicorn  # 生产 WSGI 服务器

# 前端构建
npm run build
```

### 7.2 系统依赖

```bash
# Ubuntu/Debian
sudo apt-get install -y \
    python3.11 \
    python3.11-venv \
    python3.11-dev \
    nginx \
    poppler-utils \
    libpq-dev

# CentOS/RHEL
sudo yum install -y \
    python3.11 \
    python3.11-devel \
    nginx \
    poppler-utils \
    postgresql-devel
```

---

## 八、依赖版本锁定

### 8.1 后端版本锁定

```bash
# 生成精确版本文件
pip freeze > requirements.lock

# 从锁定文件安装
pip install -r requirements.lock
```

### 8.2 前端版本锁定

```bash
# package-lock.json 自动生成
# 确保提交到版本控制

# 从锁定文件安装
npm ci
```

---

## 附录

### A. 依赖源配置

#### pip 国内源

```bash
# 临时使用
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple <package>

# 永久配置
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

#### npm 国内源

```bash
# 临时使用
npm install --registry=https://registry.npmmirror.com

# 永久配置
npm config set registry https://registry.npmmirror.com
```

### B. 虚拟环境管理

```bash
# 创建
python3 -m venv venv

# 激活
source venv/bin/activate  # macOS/Linux
.\venv\Scripts\activate   # Windows

# 退出
deactivate

# 删除
rm -rf venv
```

### C. 相关文档

- [部署交付文档](./04_部署交付文档.md)
- [后续维护文档](./05_后续维护文档.md)

